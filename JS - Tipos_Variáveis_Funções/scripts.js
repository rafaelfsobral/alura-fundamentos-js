// tipagem dinamica
// algumas documentações falam que é uma linguagem untyped

let minhaVar = 567;
minhaVar = "texto";
minhaVar = true;

// Multiparadigma = resolver o mesmo problema de várias formas diferentes
// Existem dois tipos de linguagem: Linguagem compilada (Precisa de um compilador para transformar o código em linguagem de máquina que 
// o computador entende. Quando for executar, será executada essa linguagem de máquina); e Linguagem interpretada (Escreve o código e vai
// ser executado por um programa (interpretador) por trás . Ex: Javascript)

// NodeJS => Não é uma linguagem. Javascript como é uma linguagem interpretada, ele precisa de um interpretador, por exemplo: o navegador
// ou a gente pode o node como interpretador. Se a gente usar o node, vamos usar o Javascript no backend, ou seja, no lado servidor para
// executar os códigos que a gente quer no servidor. Ele também traz algumas funções interessantes que o navegador traz pra gente, por 
// exemplo: o console.

// O node precisa ser instalado para ser utilizado.