const nomes = ['Ana', 'Ju', 'Leo', 'Paula']

nomes.forEach(imprimeNomes) // forEach pode receber dois parâmetros: O primeiro (função) é obrigatório. O segundo (índice) é opcional.

function imprimeNomes(nome){
    console.log(nome)
}